
import { getProjects } from "@/lib/data";
export default async function TrabalhoPage(){const projects:any[]=await getProjects(); const cats=["Brand","Product","Campaign"]; return <main className="section" style={{paddingTop:140}}><div className="container"><h1 className="title-xl">Work.</h1>{cats.map((cat)=>{const items=projects.filter((p:any)=>p.category===cat); return <section key={cat} style={{marginTop:48}}><h2 style={{fontSize:32}}>{cat}</h2><div className="grid-3" style={{marginTop:20}}>{items.length?items.map((project:any)=><a key={project.id} className="work-card" href={project.vimeo_url} target="_blank"><img className="thumb" src={project.thumbnail_url} alt={project.title} /><div style={{padding:18}}><div style={{fontWeight:700,fontSize:22}}>{project.title}</div>{project.description?<div className="muted" style={{marginTop:8}}>{project.description}</div>:null}</div></a>):<div className="card muted">No projects yet.</div>}</div></section>})}</div></main>}
